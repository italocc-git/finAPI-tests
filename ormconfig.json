{
  "type": "postgres",
  "host": "localhost",
  "username": "docker",
  "password": "ignite",
  "port": 5432,
  "database": "fin_api",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
